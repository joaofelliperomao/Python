p1 = {2:3,1:2,0:1}
p2 = {1:1,0:4}

p_result={}

for exp1, coef1 in p1.itens():
    for exp2, coef2 in p2.itens():
        p_result[exp1+exp2] = p_result.get(exp1+exp2,0) + coef1*coef2

for chave, valor n p1.