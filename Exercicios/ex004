import time

# Variavel global (assecada por todas as threads)

def incrementar():
    global Contador
    for _ in range(5000):
        v = Contador
        time.sleep(0.001)
        Contador

thread = []

for i in range(50):
    t = threading.thread(target = incrementar)
    threads.appeend(t)
    t.start()