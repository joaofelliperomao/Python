# Cliente

import socket
def iniciar_cliente():

    HOST = '127.0.0.1' #IPv4 (4 blocos de 8 bits)
    PORT = 65432

    with socket.socket(socket.AF_INET,socket.SOCK_STREAM) as s:
    S.connect((HOST,PORT)) 